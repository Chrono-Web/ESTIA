# The bind address and port used by Docker Compose on the host.
ESTIA_BIND_ADDRESS=127.0.0.1
ESTIA_HOST_PORT=3000

# Core API configuration. No secret is required during M0.1.
ESTIA_HOST=0.0.0.0
ESTIA_PORT=3000
ESTIA_LOG_LEVEL=info
