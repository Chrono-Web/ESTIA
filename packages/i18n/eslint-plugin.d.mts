import type { ESLint } from "eslint";

/** Whether a string reads like something a person reads. */
export declare function looksLikeText(value: string): boolean;

declare const plugin: ESLint.Plugin;
export default plugin;
